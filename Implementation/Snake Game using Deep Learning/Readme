Open 'Anaconda Prompt'
Create a conda environment with python 3.7
Activate the environment
the go to the file path writing: 'cd File_location'
finally type: 'python Agent.py'
That's it Game training is on!

You might face error, if libraries and modules are not installed correctly.
